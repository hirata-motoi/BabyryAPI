{
    key_vault_config           => '/etc/.secret/secret.conf',
    allowed_image_format       => [qw/jpeg png/],
    session_not_required_paths => [qw|
        /sequence/issue
    |],
    seq_table_map              => {
        user_id       => 'seq_user_id',
        family_id     => 'seq_family_id',
    },
}

